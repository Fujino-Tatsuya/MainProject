# PLAN-vision.md — 캐릭터 강조 비주얼 (블아풍 셀셰이딩)

> 맵용 `PLAN.md`와 별개인, 비주얼 셰이딩 작업의 잠긴 계획.
> 큰 작업이므로 grill → 이 문서 → **팀장 승인** → 구현 순서를 따른다.
> 상태: **승인됨 · 구현 착수** (2026-06-30) · 작업 브랜치 `feature/rendering` → 완료 후 `feature/map` 머지
> ▶ **재개 (2026-08-05): §7 참조.** 단계 1 완료·동결, 이번은 단계 2~3(조명·포스트프로세싱).
>   작업 브랜치 `feature/maprendering`.

---

## 0. 비주얼 타겟 (레퍼런스 / 컨셉아트)

### 채택 레퍼런스
- **블루아카이브 SD(치비) 인게임** — 탑다운 쿼터뷰에서 캐릭터가 또렷하게 보이는 룩.
- 우리 톤: **밝고 채도 높음**, 캐릭터만 강조.

### 블아 분석 결론 (이번 세션, 인게임 스샷 분석)
1. **아웃라인은 주역이 아니다.** 외곽선은 거의 안 보이고, 옷의 선은 텍스처 디테일. 화면공간 엣지 룩은 없음.
2. **접지 그림자(드롭섀도우)** — 발밑 짙은 그림자로 바닥에서 분리. 입체·강조의 1등 공신.
3. **배경 디포커스 + 탈채도** — 배경은 부드럽고 채도 낮게, 캐릭터만 선명.
4. **약한 림라이트** — 윤곽 옅은 광.
5. **평면적·깨끗한 음영** — 얼굴 음영 최소, 옷에만 부드러운 1~2단.

### 우리 프로젝트 컨셉아트 / 캐릭터 비율 (2026-06-30 반영)
- **캐릭터 비율 = SD/치비 약 3등신** (머리 크고 몸 작음, 블아 SD와 동급). 얼굴 비중이 크므로 **얼굴 음영을 깨끗하게** 유지하는 게 핵심(SD는 얼굴 음영이 지저분하면 바로 티남).
- **플레이어 — 원거리(징크스, 확정·3D 진행중)**: 검은 단발(보라 하이라이트), 흰 랩코트, 보라 톱, 검은 반바지·부츠. **차가운 톤.**
- **플레이어 — 근거리(가붕이)/서포터**: 컨셉 준비중. 동일 SD 비율·셰이더 적용 예정.
- **보스 2종**:
  - **웰즈** = SD 소녀(파란머리, 흰가운) → 캐릭터 셀셰이더 동일 대상.
  - **23호** = 거대 메카(주황+회색 하드서피스 장갑, 해골, 빨간 눈). 캐릭터용 셀셰이더 공유, 가벼운 매끈 밴딩만(§2.1). **PBR/정밀 금속 재질은 비목표.**
- **일반 몬스터 = 외부 에셋 구매** — Robot Sentries 3D Character Mega Pack(8종 stylized low-mid poly, URP·Unity6 호환, Asset Store EULA/Single Entity, $24.99). 자체 카툰 머티리얼 보유 → **기본 룩 유지**(셀셰이더 리머티리얼 안 함). 강조·시야 차폐만 적용(§1·§3).
- **환경 = 붉은 벽돌 오래된 공장**(발로란트풍), 쿼터뷰, 따뜻한 채도 톤.
  - → **캐릭터(차가운 톤) vs 배경(따뜻한 톤) 보색 대비**로 캐릭터가 자연 분리됨(장점). 배경 탈채도는 환경 분위기를 해치지 않게 **과하지 않게**.
- 레퍼런스: 블루아카이브 + 쿼터뷰 SD 액션(스킬 이펙트·장판 화려). → 이펙트 속에서도 캐릭터가 보이도록 **림라이트·접지그림자 비중↑**.

---

## 1. 범위 / 비범위

### 범위
- **캐릭터에만** 적용하는 셀셰이더 + 캐릭터 강조용 화면 후처리.
- 대상 캐릭터: 플레이어 근거리(가붕이)/원거리(징크스), 보스(웰즈 & 23호). **플레이어로 선검증.**
- 타겟: **URP17 (Unity 6000.3.16f1), PC(Steam)**.

### 비범위 (YAGNI)
- 스페큘러/PBR 정밀 금속 재질 — **제외**(비목표). 메카(23호)는 매끈 밴딩까지만, 스페큘러는 선택(§2.1).
- 포스트프로세스 엣지 아웃라인 — **채택 안 함**(블아 룩과 불일치, 배경까지 선이 그려짐).
- Toon Kit 2(유료) 레포 반입 — **금지**(셰이더 코드 재배포, Unity Asset Store EULA). 참고용 열람만 허용.
- **일반 몬스터(Robot Sentries 에셋) 셀셰이더 리머티리얼 — 안 함**(기본 카툰 룩 유지). 강조·차폐만(§3).
- 배경 오브젝트 전용 셰이딩 — 이번 범위 밖.

---

## 2. 구현 형태 (확정)

- **ShaderGraph + Custom Function** 방식.
  - 메인 셀셰이딩 = ShaderGraph 노드.
  - **LoS 클리핑/디밍 = HLSL Custom Function** 노드(풀 컨트롤).
- 선택 근거: 이 규모·PC 타겟에서 ShaderGraph vs 순수 HLSL **런타임 성능 차이는 무시 가능**. 대신 URP17 버전 안정성·튜닝 편의를 얻고, 핵심 LoS 로직만 HLSL로 제어. (자세한 비교는 세션 로그 참조)
- 참고 소스: MIT 라이선스 urp-toon-shader 계열(Delt06 등). 코드 복붙이 아니라 기법 참고.

### 2.1 보스 23호(메카) 처리 (확정)
- **단일 셀셰이더를 공유.** 메카엔 **가벼운 매끈 밴딩**(스텝 폭↑·경계 부드럽게)만 적용. **PBR/정밀 금속 재질은 비목표** — 스페큘러는 선택(생략 가능, 토이감 심할 때만 약하게).
- 캐릭터(피부/천)는 깔끔 평면음영. 웰즈(SD 소녀)는 캐릭터 모드 그대로.
- 이점: 셰이더 1개로 캐릭터·메카 모두 커버 → 일관성 + 머지/유지 단순.

---

## 3. 두 축 설계

### 축 A — 캐릭터 셀셰이더 (오브젝트 셰이더)
| 요소 | 방식 | 비고 |
|---|---|---|
| 평면 음영 | N·L → `smoothstep` 1~2단 밴딩 | 셀셰이딩 근간 |
| 얼굴 음영(SD) | 음영 최소·깨끗하게 유지 | 큰 얼굴 비중, 지저분하면 티남 |
| 림라이트 | 프레넬 기반 외곽광, 약하게 | 블아 수준, 이펙트 속 분리 |
| 그림자 색조 | 그림자부 검정 대신 채색 틴트 | 밝고 채도↑ 톤 유지 |
| 메카(23호) | 가벼운 매끈 밴딩(스페큘러 선택) | PBR 비목표, 캐릭터와 단일 셰이더(§2.1) |
| LoS 클리핑 | `_LosTex` 샘플 HLSL Custom Function → 시야 밖 디밍/클리핑 | 기존 3단계 연계 |
| 아웃라인 | 얇은 인버티드 헐 토글, **선택/최하 우선순위** | 블아는 약함 |

### 축 B — 캐릭터 강조 (기존 `Assets/1.Scripts/Rendering/Fog/` 풀스크린 패스 확장)
| 요소 | 방식 | 비고 |
|---|---|---|
| 접지 그림자 | 실시간 섀도우 우선(블롭은 탑다운 폴백) | 정면뷰 robust(§3 모드) |
| 배경 톤다운·탈채도 | 기존 LoS/층 디밍 재활용·조정 | 인프라 보유 |
| 배경 디포커스 | 캐릭터 외 영역 약한 블러(depth/레이어 기반) | 캐릭터만 선명 |

### 축 C — 일반 몬스터 (외부 에셋, Robot Sentries)
- **기본 카툰 머티리얼 유지** — 우리 셀셰이더 적용 안 함.
- 축B 전역 강조(접지그림자·배경 톤다운) + **시야 차폐(LoS)** 적용 대상.
- 시야 차폐 = 메모리 2단계 `VisionVisibility`(렌더러/네임플레이트 토글+알파)로 시야 밖 은닉. **픽셀 클리핑(3단계)은 캐릭터 우선**, 몬스터는 토글/디밍 수준으로 충분(필요 시 후속).

### 카메라 / 씬 모드 대응 (전투 탑다운 vs 하우징 자유카메라)
**하우징 씬(블아式)**: 플레이어 자유 이동 + 카메라 확대/회전 → 캐릭터를 **탑다운뿐 아니라 정면 등 모든 각도**에서 본다. 렌더링이 각도에 안전해야 함.
- **셀셰이더(축A)는 카메라 각도 독립**(N·L 기반) → 모든 뷰 동작. 단:
  - 라이트를 탑다운 전용으로 치우치지 않게(정면뷰 얼굴 안 어둡게). 캐릭터 기준 키/필 라이트.
  - **얼굴 음영 각도 robust**(블아式 페이스 컨트롤) — 정면 확대 시 깨끗 유지.
  - 림라이트·인버티드헐 아웃라인은 정면뷰에서도 안정(화면공간 아님).
- **축B 강조 + LoS는 모드별 분리**:
  - 전투(탑다운): LoS 시야제한 ON, 배경 톤다운/디포커스 ON.
  - 하우징(자유카메라): **LoS OFF**(전 영역 보임), 배경 강조 약하게/별도 프로파일.
  - → 강조·LoS 파라미터를 **씬/모드 프로파일로 토글**(FogProfile 확장).

---

## 4. 작업 순서 (이 순서대로 단계별 검증)

1. **캐릭터 셀셰이더 코어** — 평면음영 + 림라이트 + 그림자 색조 + 얼굴 깨끗. **원거리 플레이어(징크스)로 선검증** → 동일 셰이더로 웰즈 적용, 23호엔 **금속 토글 ON**(§2.1) 검증.
2. **접지 그림자 + 배경 톤다운/탈채도** — 기존 디밍 활용.
3. **배경 디포커스(블러)**.
4. **LoS 픽셀 클리핑 통합** — 기존 3단계 재개(`_LosTex` clip).
5. **아웃라인(얇게)** — 선택적, 마지막.

---

## 5. 리스크 / 검증

- **MCP 브릿지 끊김** → Unity 창 포커스로 재연결(`ref-unity-mcp-bridge-path`).
- **검증 환경** = `MainProject`(feature/map, Unity가 여는 곳). 단계마다 **Unity 콘솔 에러0 + 육안 확인**.
- **머지** = `.shadergraph`는 JSON+GUID라 머지 어려움 → 셰이더 파일 동시 편집 금지, 단일 담당.
- 작업 브랜치 = **`feature/rendering`**(이 worktree). 완료·검증 후 `feature/map`과 머지.
- 변경 파일 예상: `Assets/1.Scripts/Rendering/` 하위(포그 패스 확장) + 신규 셰이더/머티리얼 경로(셀셰이더).

---

## 6. 완료 조건 (DoD)

- [ ] 1~4단계가 플레이어 캐릭터에 적용되어 Unity 콘솔 에러0, 육안으로 "캐릭터가 배경과 분리되어 또렷".
- [ ] **정면 확대(하우징 가정) 시에도 얼굴 음영·림라이트 자연스러움** — 탑다운 전용으로 깨지지 않음.
- [ ] 전투/하우징 모드 전환 시 LoS·배경 강조가 프로파일로 올바르게 토글.
- [ ] 시야 밖 캐릭터가 LoS 클리핑/디밍으로 자연스럽게 가려짐(전투 모드).
- [ ] 60fps 기준 프레임 영향 측정(ProfilerHUD/Window) — 회귀 없음.
- [ ] feature/rendering → feature/map 통합·커밋, 팀장 리뷰.

---

## 7. 재개 (2026-08-05) — §4 단계 2~3 = 조명 + 포스트프로세싱

> 상태: **승인 대기**. 브랜치 `feature/maprendering`.
> §4 의 1단계(캐릭터 셀셰이더 코어)는 `e5cc012`·`0522731` 로 완료·**동결**됐다.
> 이번 세션 범위 = **2단계(접지 그림자·배경 톤) + 3단계(배경 디포커스)** 를 조명/포스트프로세싱으로.
> 배경 머티리얼 재정리(§1 비범위)와 HUD 배치는 이번에도 범위 밖이다.

### 7.1 새 레퍼런스 (2026-08-05, 팀장 제시)

AI 생성 이미지 1장 — 탑다운 쿼터뷰 항만/공장 구역. 목표 특징:
따뜻한 **국소 조명**(가로등·창) / 부드러운 **접지 그림자** / 물·금속 반사 /
**원경 디포커스** / 배경 소품 **채도 높음**.
🔴 이미지가 아직 채팅에만 있다 — `Docs/design/` 에 저장해야 이후 세션이 기준으로 쓸 수 있다.

⚠️ **§0·§3 의 "배경 탈채도" 와 충돌한다.** 새 레퍼런스의 배경은 채도가 살아 있고,
캐릭터 분리는 채도차가 아니라 **디포커스 + 접지 그림자 + 국소광 대비**로 이뤄진다.
→ 배경 탈채도는 **보류**하고 분리는 디포커스·접지그림자로 얻는다. **팀장·아트 확인 필요.**

### 7.2 실측한 현재 상태 (2026-08-05 · `4.MapScene`)

| 항목 | 현재 | 레퍼런스 대비 |
|---|---|---|
| 조명 | **디렉셔널 1개뿐**(intensity 1, 따뜻한 색, Soft). 맵·소품 프리팹의 Light 컴포넌트 **0개** | 🔴 가로등·창 국소광 전무 |
| 포스트프로세싱 | 씬 볼륨 프로파일에 **Bloom 하나뿐**(threshold 0.9 / intensity 1) | 🔴 톤매핑·컬러그레이딩·DoF 전무 |
| 컬러 그레이딩 모드 | `PC_RPAsset` = **LDR** | 🔴 HDR 켜 놓고 LDR 그레이딩 → 하이라이트 롤오프 없음 |
| 앰비언트 | Skybox 모드 + 기본 스카이박스, `Sun` 미지정 | 회색 하늘빛이 전체를 평평하게 덮음 |
| SSAO | 활성 / Interleaved Gradient / Intensity 0.4 / Radius 0.3 | 과거 "AO 크리즈 노이즈 얼룩" 롤백의 유력 원인 |
| 그림자 | 거리 50 / 4 캐스케이드 / 소프트 지원 | — |
| 렌더러 피처 | Decal · `FogRendererFeature`(커스텀) · SSAO | — |
| 빌트인 포그 | 꺼짐(커스텀 풀스크린 포그가 담당) | — |

**결론: 조명이 부족한 게 아니라 조명 설계가 없다.** 디렉셔널 1개 + 앰비언트로 평평하게
깔린 상태이고, 포스트프로세싱이 Bloom 하나뿐이라 톤 자체가 잡혀 있지 않다.

### 7.3 순서 (각 단계마다 스크린샷 판정)

| # | 작업 | 근거 |
|---|---|---|
| 1 | **그레이딩 기반** — `PC_RPAsset` ColorGradingMode LDR→HDR + 볼륨에 Tonemapping 추가 | 이게 없으면 이후 조정이 전부 클리핑 위에서 이뤄진다 |
| 2 | **키 라이트 + 앰비언트 재설계** — 디렉셔널 각도·강도·색, 앰비언트 모드/색 | 전체 명암 골격 |
| 3 | **국소 조명** — 가로등·창 포인트 라이트 | 레퍼런스 대비 최대 격차 |
| 4 | **컬러 그레이딩** — 색온도·채도·대비 | 레퍼런스 톤 정합 |
| 5 | **배경 디포커스(DoF)** — §4 단계 3 | 탑다운이라 카메라 거리 고정 = 튜닝 쉬움 |
| 6 | **접지 그림자 + SSAO 재검토** — Blue Noise 전환 검토 | §4 단계 2. 과거 롤백 건 재도전 |

### 7.3-a 정정 (2026-08-05, Play 1회 후) — 끌 대상은 차폐가 아니라 포그 매니저였다

처음에 **벽 투명화를 껐는데 이건 오판이었다.** 팀장 판단: 플레이어를 가리는 벽을 비우는
기능은 **살려야 한다** — 화면에 보이는 디더(스크린도어) 무늬는 품질 결함이 아니라
**의도된 비용 절충**이다(반투명 블렌딩 대신 `clip()` 으로 불투명 큐 한 패스에 끝낸다).
→ `WallOcclusionDriver` 복원. 앞으로 A/B 비교 때만 잠깐 끄고 반드시 되돌린다.

**실제로 화면을 어둡게 만든 것은 `FogManager` 였고, 그중에서도 포그가 아니었다.**
씬 값이 `fogEnabled: 0` 이라 포그는 애초에 안 그려지고 있었다. 범인은
`dimEnabled: 1` + `losEnabled: 1`(`losMaxDist: 4`) — 디밍과 시야 제한이다.
→ `FogManager` 컴포넌트 OFF. `FogRendererFeature` 가 `FogManager.HasActiveInstance` 로
조기 반환하고 `OnDisable` 이 셰이더 전역을 0 으로 되돌리므로 자기완결적으로 빠진다.
`_LosTex` 를 포그 셰이더 밖에서 샘플하는 코드는 0건이라 딸려 깨지는 것도 없다.

⚠️ 시야 제한이 꺼졌으므로 **먼 거리의 적이 그냥 다 보인다.** 게임플레이에 영향이 있는
변경이며, LoS 를 되살릴지 여부는 별도 판단이 필요하다(§4 단계 4 = LoS 픽셀 클리핑).

**물 방향 변경**: 색만 올리는 것으로는 부족하다는 판단. 셰이더를 통째로 바꾸는 것까지
열려 있다(파티클 / 단일 텍스처 / 외부 에셋). 지금은 기존 셰이더에서 밝기만 올려 두고,
접근 방식은 이후 결정한다.

### 7.3-b 🔴 포스트프로세싱이 카메라에서 꺼져 있었다 (2026-08-05)

**`MainCamera.prefab` 의 `m_RenderPostProcessing: 0`.** 즉 7.3 단계 1에서 넣은 톤매핑·
컬러그레이딩·DoF·비네트와 **원래 있던 Bloom 까지 전부 렌더되지 않고 있었다.**
"적용했는데 화면이 그대로"의 진짜 원인이며, 볼륨 프로파일만 들여다봤으면 영원히 못 찾는다.

찾기 어려웠던 이유 — **게임플레이 카메라가 씬에 없다.** `4.MapScene` 의 Camera 컴포넌트는
0개이고, `CameraTargetSwitcher.cs:132` 가 `CameraSwitcher.prefab` 에 물린
`MainCamera.prefab` 을 **런타임에 Instantiate** 한다. `MainCamera.prefab` 자체는
`CamaraScene`·`PlayerScene`(테스트 씬)에서만 직접 쓰이므로, 씬만 훑으면 게임에서 실제로
쓰이는 카메라 설정에 영원히 도달하지 못한다.
→ **카메라 설정을 의심할 때는 씬이 아니라 `CameraSwitcher.prefab` → `MainCamera.prefab` 을 볼 것.**

### 7.3-c 벽 투명화 얼룩 — 디더 애니메이션 + TAA (2026-08-05)

검은 점의 정체: `clip()` 은 픽셀당 0/1 이라 중간값이 없다. `minimumOpacity 0.15` 면
가장 많이 비워지는 지점에서도 **픽셀의 15% 가 원래 밝기로 살아남고**, 벽이 어두워서
그게 검은 점으로 읽힌다. 즉 "반투명"이 아니라 "듬성듬성 뚫린 불투명"이다.

- **디더에 프레임별 오프셋 도입** — `_WallOccDither.x`(= `frameCount % 64 * 5.588238`,
  IGN 표준 시간축 상수). `WallOcclusionGlobals.Apply` 가 매 프레임 넣는다.
- **카메라 TAA 켬** (`m_Antialiasing: 0 → 3`). 프로젝트에 안티에일리어싱이 아예 없었다
  (MSAA 도 off) — 계단현상이 그대로 나오던 것도 같은 원인이다.
- ⚠️ **둘은 세트다.** 디더가 고정이면 TAA 가 매 프레임 같은 값을 평균내 아무것도 녹지
  않고, TAA 없이 디더만 흔들면 화면이 지글거리기만 한다.
- **깊이·그림자 패스는 고정 패턴 유지**(`ClipWallOcclusionStable`). 이 패스들의 결과는
  `_CameraDepthTexture` 로 나가 SSAO·DoF 가 샘플하고 TAA 도 리프로젝션 기준으로 쓴다.
  깊이 구멍이 프레임마다 움직이면 그 효과들이 깜빡이고, TAA 는 이웃 클램프가 샘플을
  계속 기각해 오히려 누적이 안 된다. **흔들 대상은 눈에 보이는 색뿐이다.**
- 미검증: 대시처럼 빠른 이동에서의 TAA 고스팅. 거슬리면 2안 = MSAA + `AlphaToMask`.

### 7.3-d 배경 디포커스는 DoF 가 아니라 화면공간 마스크 블러로 간다 (2026-08-05)

**왜 DoF 를 버렸는가.** DoF 는 깊이로 판단하고 화면 위치를 모른다. 탑다운에서 좌우 외곽은
중앙과 깊이가 거의 같아 **원리적으로 흐려지지 않는다.** Bokeh 로 바꿔도 흐려지는 곳은
"가까운 아래 / 먼 위"뿐이다. 목표한 그림(중앙 선명 → 사방 흐림)은 선명 영역을
**화면 좌표로 직접 지정**해야 나온다. → `DoF = Off`, 마스크 블러로 대체.

**선명 영역은 화면 중앙 고정이다.** 카메라가 플레이어를 따라가므로 결과적으로 플레이어를
따라가는 효과가 된다 — 매 프레임 투영하는 추적 스크립트가 필요 없다.

**구성** (`Assets/1.Scripts/Rendering/MaskBlur/`)

| 조각 | 역할 |
|---|---|
| `MaskBlur.shader` | 3패스: 다운샘플+가로 블러 → 세로 블러 → 마스크 합성 |
| `MaskBlurFeature` | 렌더러 피처. 주입 시점 `BeforeRenderingPostProcessing`(550) |
| `MaskBlurController` | 씬 상주 게이트. 이 컴포넌트가 있는 씬에서만 돈다 |
| `MaskBlurSettings` | 튜닝값 (`Assets/99.Settings/MaskBlurSettings.asset`) |

**설계 판단 근거**

- **별도 피처로 뺐다.** 포그 패스에 얹으면 `FogManager` 를 켜야 하고 디밍·시야 제한이
  함께 돌아온다.
- **씬 게이트를 뒀다.** 렌더러 피처는 `PC_Renderer` 를 쓰는 모든 카메라에 걸리므로,
  게이트가 없으면 타이틀·로비·결과 화면까지 외곽이 흐려진다. `FogRendererFeature` 가
  `FogManager.HasActiveInstance` 로 같은 문제를 푸는 것과 동일한 패턴이다.
- **주입 시점 550** — 투명 이후라 스킬 장판·VFX 가 배경과 함께 흐려지고, 포스트프로세싱
  이전이라 블룸·톤매핑이 합성 결과 위에 걸린다.
- **반해상도 분리형 가우시안.** 배경 디포커스는 저주파라 해상도를 깎아도 티가 안 난다.
  풀해상도 단일 박스블러는 더 비싸고 품질도 나쁘다.
- **모양 = 초타원**(지수 하나로 타원↔사각 연속 조절) + 텍스처 오버라이드 슬롯.
  "원/사각/다각형 중 무엇" 을 지금 확정하지 않아도 되게 했다.
- **탈채도·암부를 같은 패스에** 넣었다. 이미 화면을 읽은 참이라 거의 공짜이고 §3 의
  "배경 톤다운"이 이 자리다. 새 레퍼런스는 배경 채도가 살아 있으므로 **기본 0**.
- 🔴 **셰이더 참조를 피처에 직렬화했다.** `Shader.Find` 만 쓰면 빌드에서 스트립된다
  (미니맵 전례). `PC_Renderer.asset` 의 `_shader` 필드가 참조 체인이다.
- 🔴 **렌더러 피처는 서브에셋 등록이 필수다.** `AddObjectToAsset` 없이 목록에만 넣으면
  저장 시 null 로 직렬화된다(`VolumeProfile.Add` 와 같은 함정). 추가 후
  `m_RendererFeatureMap` 이 features 개수와 맞는지 확인할 것 — 48자→64자(3→4개)로
  재계산된 것을 검증했다.

**미검증**: Play 육안. 특히 `size`(0.30/0.34)·`feather`(0.35)·`blurStrength`(1) 는
전부 추정값이고 한 번도 화면으로 확인하지 않았다.

### 7.4 리스크

- 🔴 **국소 조명을 아트 프리팹에 넣으면 SVN 커밋이 필요하다**(`50.Art` = SVN, git 으로 안 넘어감).
  1차는 **씬 / 스포너 쪽에서** 라이트를 얹는 방향을 우선 검토한다.
- ⚠️ **추가 광원 예산** — `PC_RPAsset` 은 `AdditionalLightsPerObjectLimit 4` + 추가광 그림자 ON.
  리슨 서버 호스트가 시뮬레이션까지 지므로 광원 수·그림자 캐스팅을 함께 본다.
- ⚠️ **ColorGradingMode HDR 전환은 전 화면 룩을 바꾼다** — 동결된 툰 값도 영향받는다.
  캐릭터·무기 재조정은 허용됨(팀장 확인 2026-08-05, 현재 값이 `development` 에 남아 롤백 가능).
- ⚠️ 이번 범위는 **셰이더 신규 제작이 없어** 빌드 스트립 위험이 없다. 범위가 늘어나면
  머티리얼 → 인스펙터 참조 체인을 반드시 건다(미니맵 전례).
- ✅ `Mobile_RPAsset`·`Mobile_Renderer` 는 QualitySettings 어느 레벨에도 배정돼 있지 않아 **미사용**이다.
  `PC_RPAsset` 만 고치면 된다 — 양쪽 맞추느라 시간 쓰지 말 것.

### 7.5 검증 / 완료 조건

- 판정 = **Play 스크린샷 ↔ 레퍼런스 나란히 놓고 팀장 + 아트 육안**.
- 🔴 **고정 촬영 지점을 먼저 정한다.** 매번 다른 위치를 찍으면 단계별 비교가 성립하지 않는다.
- [ ] 단계마다 Unity 콘솔 에러 0.
- [ ] 프레임 회귀 측정(`ProfilerHUD`) — 특히 3단계(국소 조명) 전후.
- [ ] 변경 예상 파일: `Assets/99.Settings/PC_RPAsset.asset` ·
      `Assets/0.Scenes/MainFlow/4.MapScene/Global Volume Profile.asset` ·
      `Assets/0.Scenes/MainFlow/4.MapScene.unity`(조명) · `Assets/3.Materials/Toon/*.mat`(필요 시).
