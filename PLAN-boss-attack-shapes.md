# PLAN-boss-attack-shapes.md — G4 + 공격 범위/회전 재작업 (2026-09-18)

> 상태: **승인 대기.** 담당 경석. 브랜치 `feature/Boss23`.
> G4 상세는 [PLAN.md](PLAN.md) §3-A 에 실측·순서·함정이 이미 있다 — **재측정 불필요**.
> 미니맵 작업은 [PLAN-minimap.md](PLAN-minimap.md) 로 보류(팀원 CombatHUD 푸시 대기).

## 1. 목표

팀장 플레이 피드백 6건을 처리한다. 핵심은 **"예고를 보고 피할 수 있게"** 만드는 것.

## 2. 현재 이해 — 실측 (2026-09-18)

### 예고와 판정은 이미 같은 칸에서 나온다 ✅

```csharp
// TwentyThreeBoss.cs:1003  — coneRadius 가 있으면 판정도 부채꼴. 앵커 콜라이더가 아니다.
if (e == null || e.coneRadius <= 0f) return meleeAttack.Hit();
return meleeAttack.HitCone(transform.position, ConeForward(e), e.coneRadius, e.coneAngle);
```

훅·어퍼·잡기는 `coneRadius`/`coneAngle`/`coneOffsetAngle` 이 **예고와 판정을 동시에** 정한다.
`Hand_L`/`Hand_R` 의 BoxCollider(2.6³)는 이 경로에서 **쓰이지 않는다**(폴백·돌진 실측용).

### 현재 SO 값 (`No23.asset`)

| 공격 | lungePathRadius × lungeDistance | coneRadius | coneAngle | offset |
|---|---|---:|---:|---:|
| 훅 L (0) | 1.2 × 2.5 | 4.42 | 120 | **−30** |
| 훅 R (1) | 1.2 × 2.5 | 4.42 | 120 | **+30** |
| 어퍼 (2) | 1.2 × 2.5 | 4.08 | **180** | 0 |
| 잡기 (3) | — | 8 | 90 | 0 |

셋 다 **이미 띠 + 끝점 부채꼴 2조각**을 그린다. 어퍼 레퍼런스(띠 + 끝점 **원형**)와 다른 건
`coneAngle` 이 180(반원)이라는 점뿐이다.

### 돌진

```
dashDuration 0.7 × moveSpeed 2.5 × dashSpeedMultiplier 6 = 10.5m
min(dashMaxDistance 16, 10.5) = 10.5m      ← 시간이 먼저 끝난다
```

🔴 **`dashMaxDistance` 는 지금 구속하지 않는다.** 이 칸만 올리면 아무 변화가 없다.
폭은 SO 에 칸이 없다 — `TwentyThree.prefab` 의 `DashBody` `m_Size: {3.4, 3.4, 4.42}` 에서 읽는다
(`TryGetDashFootprint`, `halfExtents` 는 `lossyScale` 반영된 월드값).

🔴 **버그**: `halfWidth = Max(halfExtents.x, halfExtents.z)` 가 **폭(x) 대신 깊이(z)** 를 집는다.
`max(1.7, 2.21) = 2.21` → 예고 띠 폭 **4.42m** vs 실제 판정 폭 **3.4m**. 예고가 30% 넓다.

### 회전

```csharp
// TwentyThreeBoss.cs:1382  case BossAttackPhase.Telegraph:
//   "부채꼴은 매 프레임 보스 정면에서 다시 그려지므로 예고도 함께 돈다 —
//    플레이어는 부채꼴이 자기를 향해 도는 것을 보고 빠져나갈 수 있다."
FaceTarget();
```

**의도된 동작이고, 이번에 뒤집는다.** 팀장 피드백: *"회전하니까 피할 수가 없어."*
🔴 그냥 지우면 안 된다 — `turnSpeed: 10` 이라 한 프레임 Slerp 로는 몇 도밖에 못 돌아
**보스가 엉뚱한 데를 때린다**(주석이 경고하는 함정, 2026-08-18 에 이 문제로 생긴 코드다).

### 돌진 클립

`DashAttack` 스테이트는 `Boss_23_dash`(internalID `4043419722265811029`)를 물고 있다.
목표 `Boss_23_dash.001` 은 같은 FBX 안, internalID `-3181347391771587835`.

| 클립 | 프레임(60fps) | 길이 | loopTime |
|---|---|---:|---|
| `Boss_23_dash` (현재) | 17→102 | 1.42s | **1 (루프)** |
| `Boss_23_dash.001` (목표) | 0→158 | **2.63s** | **0 (원샷)** |

🔴 `dashDuration` 0.7 초인데 `.001` 은 2.63 초 **원샷**이다. 그냥 바꾸면 앞 27% 만 보이고
루프 폴백도 없다. **재생속도는 클립 길이에서 역산한다**(PLAN.md §3-A 와 같은 함정).

## 3. 확정 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | 돌진 **폭만** 1.3배 — `DashBody` `m_Size.x` **3.4 → 4.42** | 팀장 확정. 길이는 10.5m 유지 |
| D2 | `TryGetDashFootprint` 의 `Max(x, z)` → **`x` 만** | 예고가 판정보다 넓게 거짓말하는 것을 없앤다 |
| D3 | 어퍼컷 = 띠 + 끝점 **원형** → `coneAngle` **180 → 360** | 레퍼런스(3번째 사진). 판정도 같이 원형이 된다 |
| D4 | 훅 = **네모**. 예고·판정 둘 다 박스로 | 레퍼런스(1·2번째 사진). 한쪽만 바꾸면 거짓말이 된다 |
| D5 | 훅·어퍼·잡기 예고 중 **회전 잠금** — 예고 시작에 `FaceTargetImmediate()` 스냅 후 고정 | 2026-08-18 확정의 **뒤집기**. 스냅이 없으면 엉뚱한 데를 때린다 |
| D6 | 돌진 클립 `Boss_23_dash.001`, **재생속도는 길이에서 역산** | 상수 박기 금지 |
| D7 | G4 는 [PLAN.md](PLAN.md) §3-A 그대로 | 실측·순서·함정이 이미 확정 |

## 4. 접근 — 슬라이스 (작은 것부터, 각 슬라이스마다 Play 확인)

- **T1. 돌진 폭** (D1+D2) — 프리팹 `m_Size.x` 1줄 + 코드 1줄. 가장 얕고 즉시 눈에 보인다.
- **T2. 어퍼컷 원형** (D3) — SO 1줄(`coneAngle: 180 → 360`).
  ⚠️ 부채꼴 텍스처 생성기가 **360 을 원형으로 처리하는지 먼저 확인**(`_builtConeHalfAngle`).
- **T3. 회전 잠금** (D5) — `Telegraph` 케이스의 `FaceTarget()` 을 진입 1회 스냅 + 잠금으로.
  잡기는 `Windup` 에서도 `FaceTarget()` 을 도므로(`attackId == Grab`) 그쪽도 같이 본다.
- **T4. 훅 네모** (D4) — `BossAttackEntry` 에 박스 칸 추가 + `BossAttackConeTelegraph` 에 박스 생성 +
  `HitCone` 대신 박스 오버랩. **이번 작업에서 가장 큼.**
- **T5. 돌진 클립** (D6) — 컨트롤러 `m_Motion` 교체 + 재생속도 역산.
- **T6. G4 점프 이륙** — PLAN.md §3-A 7단계 그대로.

## 5. 네트워크 권한

전부 **서버 권한 유지**. 예고는 기존 `ClientRpc` 경로 그대로(각 피어가 따로 계산하면 갈라진다).
새 RPC 는 훅 박스 파라미터가 기존 `ShowAttackConeClientRpc(slot, growTime)` 에 실려 가므로 **0개**.

## 6. 리스크

1. **T4 가 예고·판정 두 곳을 동시에 바꾼다.** 한쪽만 나가면 "장판 밖인데 맞는" 버그가 된다.
   같은 SO 칸에서 두 경로가 읽도록 유지하는 것이 유일한 방어선.
2. **D5 는 난이도를 올린다.** 예고가 더 이상 따라오지 않으므로 훅·잡기가 움직이는 플레이어를
   놓치기 쉬워진다 — `FaceTargetWhileAttacking => false` 주석이 이미 "대가"로 적어 둔 것.
   의도한 방향이지만 **Play 로 체감 확인 필요**.
3. **T5 후 돌진 연출이 길이와 어긋날 수 있다.** 0.7초 이동 vs 2.63초 클립.
   재생속도 3.76배가 너무 빨라 보이면 `dashDuration` 을 올리는 쪽도 후보다(길이가 같이 늘어난다 —
   D1 이 "폭만"이었으므로 그때 팀장 재확인).
4. **G4 는 독립**이라 위와 충돌하지 않지만, 같은 `_stateTimer` 예산을 건드린다.

## 7. 범위 밖

- 데미지 밸런스(B2) · 고출력 게이지(B6) · 전기 지대(B7) · 직렬화 예외(B8).
- 미니맵 / CombatHUD UI.
- `dashMaxDistance` 조정 — 지금 구속하지 않으므로 손대면 혼란만 는다(주석으로 남긴다).

## 8. 완료 기준

1. 돌진 예고 띠 폭이 **판정 폭과 같다**(현재 4.42 vs 3.4 불일치 해소), 그리고 1.3배로 넓어졌다.
2. 어퍼컷 예고·판정이 **띠 + 끝점 원형**이다.
3. 훅 예고·판정이 **네모**이고 좌/우가 각각 치우쳐 있다.
4. 훅·어퍼·잡기 예고가 **시전 후 따라 돌지 않는다.** 옆으로 걸어서 피할 수 있다.
5. 회전 잠금 후에도 보스가 **엉뚱한 방향을 때리지 않는다**(스냅이 동작).
6. 돌진이 `Boss_23_dash.001` 로 재생되고 클립이 잘리거나 겉돌지 않는다.
7. G4 — 점프에 이륙 구간이 보이고 순간이동처럼 보이지 않는다.
8. 컴파일 에러 0 — `observedAssemblyCount` 확인(0 이면 검증 안 된 것 · 교훈 #106).

## 9. 검증 계획

- 슬라이스마다 `4.MapScene` 또는 보스 씬에서 Play — T1·T2·T3 은 즉시 눈으로 확인된다.
- T4 는 **예고 밖에 서서 안 맞는지** / **예고 안에서 맞는지** 양방향 확인(과소·과대 표시 둘 다).
- T5 는 돌진 1회를 녹화해 클립 구간이 잘리는지 본다.
- 🔴 `.fbx.meta` 이벤트 `time` 은 **정규화(0~1)** 다(교훈 #105). 초로 읽지 않는다.
- 🔴 프리팹·컨트롤러 수정이므로 **Play 모드면 먼저 정지**(CLAUDE.md §6).
