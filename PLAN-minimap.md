# PLAN-minimap.md — 미니맵을 생성 레이아웃과 무관하게 `minimap.png` 룩으로 (2026-09-18)

> 🟢 **재개 조건 충족됨 (2026-09-19).** 이 계획을 보류시켰던 이유는
> "팀원이 `CombatHUD.prefab` 을 미푸시 로컬로 갖고 있다"였는데, 그게 **development 에 올라왔고
> 내 브랜치에도 머지됐다**(`c44d235c`, `c603e699`). 더 이상 프리팹 충돌 위험이 없다.
>
> 🔴 **그런데 들어온 건 키가이드뿐이다.** 실측(2026-09-19):
> - `keyguide.png` 참조 **1건** — 오브젝트 이름이 그냥 **`Image`** 다(이름 안 바꿈).
> - `minimap` 문자열 **0건** — 미니맵 슬롯도 스크립트도 없다.
> - 타이머 아트(`gauge_timer`·`slot_timer`·`icon_timer_boss`) 도 전부 **0건**.
>
> → **D5·D6 은 폐기한다.** 슬롯을 이름으로 찾을 게 없으므로,
> 이제는 `CombatHUD.prefab` 에 **슬롯을 직접 만드는 게 맞다**(충돌 위험이 사라졌으므로).
> 나머지 결정(D1~D4·D7·D8)과 슬라이스 S1~S5 는 그대로 유효하다.
>
> 🟢 **2026-09-19 그릴 2차 — D9~D12 추가.** 레퍼런스 해석이 확정됐고(45° 회전), 보스 타이머가
> **범위 안으로 들어왔다**([PLAN-boss-timer.md](PLAN-boss-timer.md)). 프리팹 슬롯 저작은 두 작업이
> 공유하므로 **한 번에 끝낸다**. §7 의 "보스 타이머 = 범위 밖"은 무효다.

> 상태: **승인 대기.** 담당 경석. 브랜치 `feature/Boss23`.
> 그릴 확정: 2026-09-18. 보스 FSM 백로그는 [PLAN-boss-backlog.md](PLAN-boss-backlog.md) 에 그대로 둔다.

## 1. 목표

미니맵이 **`MapGenerator` 가 어떤 레이아웃을 뽑아도** `Assets/50.Art/UI/HUD/minimap.png` 의 룩
(둥근 회색 방 · 어두운 외곽선 · 얇은 통로)으로 보이게 한다. **미니맵 로직만.**

## 2. 현재 이해 (2026-09-18 실측)

파이프라인은 이미 서 있다. 새로 만들 게 아니라 **채움과 외곽선만 바꾸는 작업**이다.

| 조각 | 위치 | 하는 일 |
|---|---|---|
| `BakeTerrain()` | `MinimapController.cs:136` | 오쏘 카메라로 실제 지오메트리를 1024² RT 에 굽는다 → `_MainTex` |
| `BuildSilhouette()` | `:204` | 슬롯 풋프린트 + 통로 AABB 를 256² `R8` 로 CPU 생성 → `_SilTex` (**이미 스키매틱**) |
| `UpdateMask()` | `:290` | R=탐사 누적 / G=현재 시야 를 128² 에 스탬프 → `_MaskTex` |
| `MinimapUI.shader` | `Rendering/Minimap/` | 위 셋을 합성. **3단계 시야가 이미 구현돼 있다** |
| `ApplyPanelSize()` | `:459` | 패널 크기 전환 + 정적 마커 재배치 (`[` `]` 프리셋 300/400/520) |

핵심: `minimap.png` 룩에 필요한 **맵 모양(`_SilTex`)은 이미 정확히 생성되고 있다.**
지금 룩이 다른 이유는 채움이 **지형 사진(`_MainTex`)** 이고 **외곽선이 없고** 코너가 직각이기 때문이다.

## 3. 확정 결정

| # | 결정 | 근거 |
|---|---|---|
| D1 | 채움을 **균일 회색(플랫)** 으로. 지형 베이크는 **삭제하지 않고 건너뛴다**(`UseTerrainBake = false` 기본) | 되돌리기 쉽다. 베이크는 밝기 0 재시도·어비스 물 plane 등 알려진 함정이 있어 안 쓰는 편이 안정적 |
| D2 | **외곽선은 셰이더에서** `_SilTex` 이웃 4샘플 엣지로 뽑는다 | 레이아웃 무관하게 공짜로 따라온다. 아트 타일 불필요 |
| D3 | **코너 반경은 `BuildSilhouette` 에서** — `FillWorldRect` 를 둥근 사각형으로 | CPU 1회, 256² 라 비용 무시 가능 |
| D4 | **탐사 3단계 유지** — 🔴 **미탐사=어두운 채움**(D16 으로 개정) / 탐사됨=중간 / 시야=밝음 | 작동하는 시스템을 버리지 않는다 (팀장 확정) |
| D5 | 🔴 **폐기** — 이제 `CombatHUD.prefab` 에 슬롯을 **직접 만든다**. (원안: 프리팹을 건드리지 않고 이름으로 찾아 붙는다) | 폐기 사유 = 팀원 미푸시 로컬이 development 에 올라와 **머지 충돌 위험이 사라졌고**, 찾을 슬롯 자체가 없었다(실측 0건) |
| D6 | 🔴 **폐기**(위 재개 조건 참조) | — |
| D9 | **미니맵을 45° 회전한다.** 인스펙터 `MapRotationDegrees`(기본 45). 🔴 **회전은 pivot 이 중앙인 안쪽 컨텐츠 RectTransform 에 건다** | 레퍼런스(`minimap.png`)의 방이 전부 마름모인데 `ZoneSlot` 회전은 90° 배수라 월드 배치는 **축 정렬**이다. 즉 마름모는 레이아웃이 아니라 **보는 각도**다. 카메라가 요각 45° 라 돌리면 화면 방향과 미니맵 위쪽이 일치한다(팀장 확정) |
| D10 | **마커 아이콘은 -45° 역보정.** 위치만 회전에 따른다 | 아이콘이 같이 기울면 해골·퀘스트 아이콘이 비뚤어져 읽힌다 |
| D11 | **맵 바깥은 완전 투명.** 프레임·배경 패널 없음. 🔴 **셰이더 기본값이 아니라 `.mat`/런타임 값을 바꿔야 한다** | 목업 실측 — 프레임 없이 씬 위에 얹혀 있다. 45° 회전하면 패널 모서리가 드러나므로 배경이 있으면 **회전한 네모가 보인다**. `MinimapComposite.mat` 에 `_BgAlpha: 0.35` 가 **저장돼 있고** Controller 가 그 머티리얼을 복제하므로 셰이더 Properties 기본값만 0으로 바꾸면 **아무 일도 안 일어난다**(Codex 교차검증 2026-09-20) |
| D12 | 프리팹 저작은 **에디터 메뉴 스크립트**로 (YAML 직접 편집 금지) | `CombatHUD.prefab` 5165줄. 기존 선례 `BossRoomAuthoring` 등과 같은 방식. 상세는 PLAN-boss-timer T11 |
| D13 | ⚠️ **폐기됨 (2026-09-20 실측)** — 아래 "뒤집힌 결정" 참조. 원안: D4 정정 — "미탐사=외곽선만" 은 현재 동작이 아니라 새로 만들어야 한다 | 현재 셰이더는 `rgb = lerp(_SilColor, terrain, explored+visible)` 이고 알파는 `max(shape, _BgAlpha)` 라 **미탐사 내부가 `_SilColor` 로 꽉 찬다.** `lit` 식만 유지해서는 요구가 충족되지 않으므로 **미탐사 내부 알파와 외곽선 알파를 분리**해야 한다 |
| D14 | **아이콘 역보정은 동적 마커에도 적용한다** | D10 을 정적 마커에만 걸면 플레이어·팀원·몬스터 점이 기울어진 채 남는다. 동적 마커는 매 프레임 생성·갱신되므로 생성 지점에서 함께 건다 |
| D16 | 🔴 **미탐사는 "외곽선만" 이 아니라 "어두운 채움" 이다** (D13 뒤집음) | 아래 절 참조 |
| D17 | **역할 아이콘(퀘스트·스폰·보스입구)은 맵과 함께 회전**한다. 원형 마커만 정립 유지 | 아이콘 아트가 직사각형이라 정립시키면 마름모 미니맵 위에서 축이 어긋나 보인다(팀장 확정 2026-09-20). 인스펙터 `RotateRoleIconsWithMap` 로 되돌릴 수 있게 빼 뒀다 — 아트가 바뀌면 끈다 |
| D15 | 🔴 **미니맵은 HUD Canvas 의 표시 권한을 가져오지 않는다** | 기존 `UpdateCanvasVisibility()` 가 **매초 "플레이어가 한 명이라도 있으면 `_canvas.enabled = true`"** 로 만든다. 슬롯 부착 후 `_canvas` 를 HUD Canvas 로 바꾸고 이 코드를 두면 **사망으로 숨긴 HUD 전체를 되살린다.** 표시 권한은 `PlayerCombatUiLifecyclePolicy` 에 남긴다 |

### ⚠️ 뒤집힌 결정 — D13 "미탐사=외곽선만" (2026-09-20)

**무엇을 골랐었나** — 레퍼런스 `minimap.png` 의 회색을 "탐사된 방" 으로 읽고,
미탐사는 내부를 투명하게 두어 외곽선만 남겼다(`fillA = shape * seen * _FillAlpha`).

**왜 뒤집혔나** — 실제로 빌드해서 보니 **미니맵이 거의 안 보였다.** 맵 대부분이 미탐사라
화면에는 플레이어 주변 몇 칸만 떠 있었다. 팀장 판정:
> "레퍼런스에 맞춰서 저렇게 빈 상태가 아니라 어두운 형태로 안에 채워져 있고 그리고 밝아져야 하는데
>  그렇지 않으니까 지금 너무 안 보임."

**지금** — `fillA = shape * _FillAlpha` 로 **맵 전체가 늘 채워져 있고**, 밝기만 3단계로 오른다
(미탐사 0.34 → 탐사 0.62 → 시야 1.0). 즉 레퍼런스의 회색은 "탐사된 방" 이 아니라 **바탕**이었다.

**교훈** — 레퍼런스 이미지 한 장으로는 "이 회색이 어느 상태인지" 를 알 수 없다.
정적인 그림에는 **상태 변화가 안 담긴다** — 3단계 같은 동적 표현은 그림이 아니라 말로 확인해야 한다.

## 4. 접근 — 슬라이스

- **S1. 룩** — 셰이더에 `_FlatColor` / `_OutlineColor` / `_OutlineWidth` 추가, 채움을 플랫+외곽선으로.
  `_SilTex` 는 `Bilinear`/`Clamp` 유지. 3단계 밝기는 기존 `lit` 식을 그대로 쓴다.
- **S2. 코너** — `FillWorldRect` → `FillWorldRoundedRect(cornerMeters)`. 통로는 반경 0.
- **S3. 베이크 건너뛰기** — `UseTerrainBake=false` 면 카메라 생성·렌더·`RetryBake` 를 통째로 skip.
- **S4. 슬롯 부착** — `CombatHUD.prefab` 에 저작한 `MinimapSlot` 밑에 RawImage 를 만든다(D12 의 저작 스크립트가 슬롯을 만든다).
  🔴 **`CombatHUD` 는 `Player.prefab` 의 자식**이라 슬롯은 **로컬 플레이어가 스폰된 뒤에야 생긴다** —
  `Player.LocalPlayerChanged` 를 구독하고, 못 찾으면 현행 자체 Canvas 로 폴백한다.
  베이크·마스크·실루엣은 씬 상주 `MinimapController` 가 계속 들고 있으므로 **UI 만 다시 붙이면 된다**.
- **S5. 회전** — 🔴 **`_mapRect` 를 그대로 돌리면 안 된다.** `_mapRect.pivot = (1,0)`(우하단)이라
  **화면 우하단 모서리를 축으로** 돈다. 한 변 `L` 을 45° 돌리면 pivot 아래로 `L/√2` 내려가는데,
  현재 씬 값(`L=350`, 하단 여백 2)이면 **약 245.5 UI 단위가 화면 아래로 빠진다**(Codex 실측 계산).
  → **pivot 중앙인 안쪽 컨텐츠 RectTransform** 을 만들어 거기에 회전을 걸고,
  `ApplyPanelSize` 에서 **회전된 외접 영역 + 아이콘 여유**를 계산해 위치를 보정한다.
  좌표 정합성 자체는 유지된다 — 맵과 마커는 같은 부모라 함께 정확히 돈다(Codex 확인).
- **S6. 슬롯 부착 안전장치** — 부착 대상은 **`LocalPlayerChanged` 로 받은 로컬 Player 의 자식 HUD 로 한정**한다.
  전역 검색은 **원격 HUD 가 비활성화되기 전 인스턴스를 잡을 수 있다.**
  `LocalPlayer` 가 null 로 전환되면(디스폰) 붙여둔 RawImage·마커가 **Player 와 함께 파괴**되므로,
  재부착 시 텍스처뿐 아니라 **정적 마커와 참조도 복원**해야 한다.
  `EnsureUI()` 의 `_canvas != null` 조기 반환을 그대로 두면 **폴백에서 슬롯으로 옮겨가지 못한다**.

## 5. 네트워크 권한

**변경 없음.** 탐사 마스크는 기존대로 클라 로컬 계산(전 클라 동일 입력 → 동일 결과).
새 RPC·NetworkVariable **0개**. 서버 코드 미변경.

## 6. 리스크

1. **`_corridorsRoot` 이름 하드코딩** — `Awake` 가 `GameObject.Find("Stage1/Level_wall_hallway")`.
   못 찾으면 통로가 안 그려진다(경고만 뜬다). 레이아웃 룩 검증 시 **이것부터 확인**한다.
2. **`CorridorMaxSpan 60` 필터** — 맵 전체를 덮는 묶음 하나가 실루엣을 통째로 채워
   "거대한 사각형" 이 되는 기존 사고가 주석에 남아 있다. 룩이 무너지면 1순위 용의자.
3. 🔴 **슬롯 수명이 로컬 플레이어에 묶인다.** `CombatHUD` 는 `Player.prefab` 의 자식이고
   `PlayerCombatUiLifecyclePolicy` 가 `hudCanvas.enabled = (Alive || Soul)` 로 캔버스를 끈다 —
   **완전 사망하면 미니맵도 사라진다.** 팀장 브리핑 후 그대로 가기로 함(PLAN-boss-timer T10 대가 항목).
4. 룩 재현도 — `minimap.png` 는 손으로 그린 그림이라 1:1 이 될 수 없다.
   합격선은 "같은 시각 언어"(둥근 회색 방 · 외곽선 · 얇은 통로).

## 7. 범위 밖

- 키가이드 배선.
- ~~보스 타이머~~ → **범위 안으로 이동**. [PLAN-boss-timer.md](PLAN-boss-timer.md) 참조.
- **M 키 / `MapOverviewUI`** — 지우지도 고치지도 않는다. (이 머티리얼을 안 쓰므로 룩 변경이 번지지 않음 — 2026-09-19 확인)
- ~~`CombatHUD.prefab` 편집~~ → **범위 안**(D5 폐기). 단 **슬롯 2개 추가 외의 변경은 하지 않는다.**
- `.meta` 누락 5건(`gauge_HP` · `gauge_HP_noncolor` · `portrail_gunner` · `slot_cooldown*`) 배선 — 서버에 `.meta` 가 없어 팀원마다 guid 가 갈린다. 은희에게 커밋 요청 필요.
- `Detection_Fill` 배선 · 보스 FSM 백로그 B1~B10.

## 8. 완료 기준

1. `4.MapScene` 플레이 시 미니맵이 **둥근 회색 방 + 어두운 외곽선 + 얇은 통로**로 나온다.
2. `MapGenConfigSO` 시드를 바꿔 **다른 레이아웃**을 뽑아도 같은 룩이다.
3. 탐사 3단계가 그대로 동작한다 — 미탐사 외곽선만 / 탐사됨 디밍 / 시야 밝음.
4. 통로가 실제로 그려진다(리스크 1·2 가 안 터졌다).
5. 컴파일 에러 0 — `observedAssemblyCount` 확인(0 이면 아무것도 검증하지 않은 것 · 교훈 #106).
6. 🔴 **D5 가 폐기됐으므로 `CombatHUD.prefab` 은 뜨는 게 정상이다.** 대신 확인할 것:
   저작 스크립트가 만든 슬롯 2개 말고 **다른 차이가 diff 에 없어야** 한다(Unity 재직렬화 사고 감지).
7. 레퍼런스와 같은 방향으로 보인다 — 방이 마름모(D9), 프레임 없음(D11), 아이콘은 정립(D10·D14).
8. 🔴 **회전 후에도 미니맵이 화면 안에 완전히 들어온다** — `[` `]` 프리셋 3단계 전부에서(S5).
9. 🔴 **사망해도 미니맵이 HUD 를 되살리지 않는다**(D15) — 죽으면 HUD 와 함께 사라진 채로 남는다.

## 9. 검증 계획

- `unity_get_type_symbols` 로 변경 타입이 실제 컴파일된 어셈블리에 있는지 직접 조회.
- `4.MapScene` 플레이 → 1·3·4.
- 시드 2회 변경 → 2.
- 🔴 Play 모드 중이면 **먼저 정지**(CLAUDE.md §6). 셰이더·스크립트는 에디터를 켜둔 채 바꿔도 된다.
  **프리팹 저작(D12)은 Play 중에 돌리지 않는다** — 종료 시 메모리 상태가 덮어쓴다.
- 저작 후 `git diff Assets/2.Prefabs/UI/CombatHUD.prefab` 으로 **슬롯 2개 외 변경이 없는지** 확인하고,
  `.meta` guid 가 그대로인지 본다(CLAUDE.md §6 확인 절차).
