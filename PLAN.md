# PLAN.md — 미니맵 (우측 하단, 라벤스워치 룩 + 스타크래프트식 탐사) (2026-07-03)

Status: **구현 완료 (2026-07-03, 커밋 3638ad5)** — 잔여 검증: 게임뷰 육안(탐사 디밍), MPPM 팀 시야 합산, 몬스터 프리팹에 MinimapMarker 부착(에셋 확정 시)

작업지: `feature/map` (= `C:/Users/user/MainProject`, Unity 열기). 레퍼런스: 라벤스워치 우하단 미니맵.

---

## 요구사항 (grill 확정본)

| 항목 | 확정 내용 |
|---|---|
| 위치/형태 | 우측 하단 고정 사각형, 북향 고정 |
| 시야 3단계 | 미탐사=**윤곽 실루엣만** / 탐사됨=지형 그림 **0.5 디밍** / 현재 시야=**풀 표시** |
| 상시 마커 (처음부터) | 퀘스트 존, 스폰 존, 보스방 입구, 티어 1/2/3 성장 보상(노드) |
| 실시간 표시 | 내 위치(강조)+팀원, 시야 내 몬스터, 노드, 높낮이 음영 있는 지형 그림 |
| 시야 | 반경 **~15m**, **팀 공유**(탐사 마스크·몹 표시 모두 팀원 시야 합산) |
| 노드 데이터 | 보상 오브젝트 시스템 **미확정** → 마커 인터페이스만 확정, 임시로 몬스터 SpawnPoint(티어별) 위치에 표시. 추후 실제 보상 오브젝트로 이전 |

## 설계

### 1. 지형 베이크 (높낮이 그림)
- 맵 생성 완료 직후 1회: 오버헤드 orthographic 카메라가 맵 전체(슬롯 합산 bounds)를 RenderTexture로 캡처.
- 배경 알파 0으로 찍어서 **알파 = 맵 실루엣**으로 활용 (미탐사 단계 표현에 사용).
- 베이크 카메라는 FoW/포스트프로세싱 제외(컬링 마스크·포그 off).

### 2. 시야/탐사 마스크 (스타크래프트식)
- 저해상도 RT(256², R=탐사 누적 / G=현재 시야) — 매 틱 모든 플레이어 위치에 반경 15m 소프트 원 스탬프.
  G는 매번 클리어(현재 시야), R은 누적(지나간 곳).
- 클라 로컬 계산(팀원 위치는 NGO로 이미 복제됨 → 모든 클라에서 동일 결과, 추가 네트워크 없음).

### 3. 미니맵 셰이더 (RawImage용)
- `미탐사`: 실루엣 단색(베이크 알파 기반) / `탐사`: 베이크색 × 0.5 / `현재 시야`: 베이크색 × 1.0 — 마스크 R/G로 lerp.

### 4. 마커 시스템
- `MinimapMarker` 컴포넌트(타입: Quest / Spawn / BossGate / NodeT1·T2·T3 / Monster / Player / Ally) — 붙이면 자동 등록/해제.
- 상시 마커: 맵 생성 시 슬롯 역할(퀘스트/스폰/보스입구)에서 자동 생성. 아이콘은 기존 `MapObj/texture/Quest.png, Spawn.png, Boss.png` 재사용.
- 노드 마커: SpawnPoint 티어(1/2/3)에 임시 부착 — 보상 시스템 확정 시 컴포넌트만 옮기면 됨.
- 상시 마커는 탐사와 무관하게 항상 표시 / 몬스터 마커는 팀 시야 15m 내에서만 표시.
- 좌표 변환: 월드 XZ → 맵 bounds 정규화 UV → 미니맵 RectTransform.

### 5. 파일 구성 (신규)
- `Assets/1.Scripts/Map/Minimap/MinimapController.cs` — 베이크/마스크/UI 총괄
- `Assets/1.Scripts/Map/Minimap/MinimapMarker.cs` — 등록형 마커
- `Assets/1.Scripts/Rendering/Minimap/Minimap.shader`(or ShaderGraph) — 3단계 합성
- MapScene에 MinimapCanvas(우하단) + 베이크 카메라 프리팹

## 구현 순서 / 검증
1. 베이크 + 우하단 UI 표출 (전체 밝게) → 에디터 Test Generate로 확인
2. 탐사 마스크 + 3단계 셰이더 → 플레이 모드에서 걸어다니며 디밍 확인
3. 마커(상시 4종 + 플레이어/팀원/몬스터) → 플레이 모드 확인
4. MPPM 2인: 팀원 시야 합산·팀원 아이콘 확인
- 완료 조건: 위 4단계 검증 통과 + 에러 0 + 커밋(기능 단위 1~2건)

## 리스크 / 확인 필요
- 기존 FoW(스크린스페이스 포그)·시야컬링과 베이크 카메라 간섭 — 컬링 마스크로 격리 예정
- 몬스터 위치 접근: 씬의 복제된 NetworkObject 대상, 태그/컴포넌트 기준 스캔 (UnitBase 계열 확인 후 결정)
- 미니맵 크기/여백 수치는 1차 구현 후 스샷 보고 튜닝
