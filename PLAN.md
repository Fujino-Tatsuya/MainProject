# PLAN.md — 맵 생성 재작성 (사전 디자인 존 + 위치 셔플)

> 상태: **승인 대기** (grill 완료). 코어 유지 + 배치 로직만 재작성.
> 설계 SoT: 승인 후 `Docs/tech/map-generation.md` 갱신.

## 목표
Stage1 고정 지오메트리 위에서, **존 내부를 미리 레벨디자인(프리팹)으로 완성**해 두고, 매 런마다 **같은 크기 전투 존끼리 위치만 셔플**해 배치한다. 디자인(컨셉/노드/몬스터/테마)은 고정, 위치만 달라진다. 절차적 노드·장애물 배치 폐기.

## 존 모델
- **크기 3종**: 대형 / 중형 / 소형. 각 존은 `Size` + **역할 후보 플래그**(Quest/Boss/Spawn — 기존 `ZoneDefinitionSO` 플래그 재사용).
- **역할 존(보스 / 스폰=상점 / 퀘스트) = 각자 단일 고정 디자인** (셔플 안 함). 후보 중 1곳씩 역할 배정.
- **전투 존 = 크기별 고정 디자인 세트를 위치만 셔플** (Q1=(a)). 같은 크기 슬롯끼리 1:1 셔플 배치.
- **카운트는 데이터 주도** — 코드에 안 박음. Stage1의 크기별 존 슬롯 수 + 카탈로그의 크기별 디자인 수를 읽어 처리. 기획이 디자인에서 개수·노드구성·위치를 직접 조절.
  - 현재 기획 의도(예시): 대형 3 · 중형 3 · 소형 3 전투 디자인 + 역할존(보스/스폰/퀘스트). 중형 예: 2티어 1 + 3티어 5, 소형 예: 2티어 1 + 3티어 3.
- 장애물 제거. **노드(1/2/3티어) · 몬스터 스폰위치 · 몬스터 그룹 · 테마 전부 디자인 프리팹에 내장.**

## 데이터 모델 (프리팹 방식 확정)
- **`ZoneLayout` 프리팹(존 단위, 자기완결):** **바닥 + 벽 + 테마(랜드마크)** + 노드(1/2/3티어) + 몬스터 스폰위치 + `MonsterGroupID`까지 포함한 완성 디자인. (바닥·벽이 테마별로 달라서 존 프리팹에 포함)
  - 태그: `Size`(대/중/소), `Difficulty`(밴드), `Role`(Combat/Quest/Boss/Spawn).
  - **표준 소켓 규약(크기별 통일):** 같은 크기 프리팹은 동일한 풋프린트·피벗·출입구(소켓) 로컬 좌표를 따름 → 어떤 슬롯에 넣어도 다리와 자동 정렬.
- **`ZoneLayoutCatalogSO`:** (Size × Difficulty) 전투 디자인 세트 + 역할별 고정 디자인 목록.
- **Stage1 = 스켈레톤만:** 외곽 프레임 + 존↔존 **연결 다리/통로(공유·고정)** + **슬롯 앵커**(위치·방향·Size·역할후보 플래그). 바닥·벽은 존 프리팹으로 이동. 생성기는 선택된 존 프리팹을 슬롯 앵커에 `Instantiate(pos, rot)`.
- **네트워킹:** 존 시각물(바닥/벽/소품)=양쪽 로컬 생성(같은 시드·결정적), `NetworkObject`(몬스터/상호작용)=서버 스폰→NGO 복제. (기존 `MapContentSpawner` 패턴)

## 파이프라인 (채우는 순서)
1. **역할 배정:** 퀘스트 → 보스 → 스폰 (각 후보 중 1, 시드 기반). [기존 `AssignZoneRoles` 재사용]
2. **역할 존 배치:** 보스/스폰/퀘스트 각자 **고정 디자인** 배치.
3. **전투 존 채우기 (대형 → 중형 → 소형):** 각 크기마다 (Size, 현재 Difficulty) 디자인 세트를 **시드 셔플 → 같은 크기 슬롯에 1:1 배치**. (위치만 랜덤, 디자인 고정)
4. 서버에서 `NetworkObject` 스폰(기존 패턴) → NGO 복제 → 오버뷰 UI 갱신.

## 핵심 결정
- (a) **고정세트·위치셔플** / 역할존 **단일 고정** / **spawn = shop** / 카운트 **데이터 주도**.
- 난이도 = `DifficultyLevel`(Ascension + Stage 유지)로 **(Size,Difficulty) 디자인 세트 선택**. (마릿수 스케일 폐기, 몬스터는 디자인 내장)
- 범위 = 코어 유지(역할배정 · 시드RNG · `NetworkSync` 서버스폰 · 오버뷰UI), `NodePlacer`/`ObstaclePlacer` → **`LayoutPlacer` 신규**로 교체. **Stage1은 스켈레톤(프레임+다리+슬롯앵커)으로 재구성** — 바닥/벽은 존 프리팹으로 이동.

## 이번 세션 WIP 정리
- `DifficultyTableSO` / `Generate(seed, int level)` 골격: **레벨 입력은 유지**, 테이블 용도를 "마릿수"→"디자인 세트 선택"으로 변경. `MonstersPerNode` · 장애물 필드 폐기.

## 리스크 · 메모
- 난이도별 디자인 세트를 다 만드는 제작 비용 ↑ → 초기엔 **단일 난이도 세트**로 시작하고 밴드는 나중에 확장 가능(구조는 미리 둠).
- 역할 존도 추후 변형 풀로 바꾸고 싶으면 카탈로그에 Role 풀만 추가하면 됨(현재는 고정).
- **회전/미러**는 소켓 대칭이 필요 → 일단 **고정 방향 배치**, 회전은 옵션(나중).
- **Stage1 재구성**(바닥/벽 → 존 프리팹 이관 + 슬롯 앵커/다리 정리)은 기획 제작 작업 동반. 표준 소켓 규약(크기별 풋프린트·출입구 좌표) 먼저 확정해야 프리팹 양산 가능.

## 범위 외
- 실제 몬스터 밸런스, 테마 아트 제작, 난이도 밴드 실수치.

## 완료 기준
- `LayoutPlacer`가 역할배정 → 역할존 고정배치 → 전투존 크기순 셔플배치 → 서버스폰까지 동작. `ObstaclePlacer`/`NodePlacer`·장애물 제거. 오버뷰 UI 반영.
- 카운트 하드코딩 없음(슬롯/카탈로그 수에서 유도). `Generate(seed, level)`이 난이도로 디자인 세트 선택.

## 검증
- 에디터 Test Generate로 매 시드 다른 **위치 셔플** 확인 + 콘솔 로그. (Unity MCP 복구됨 → 라이브 검증 가능)
