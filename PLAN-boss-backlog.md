# PLAN-boss-backlog.md — 23호/웰즈 보스 남은 작업 (2026-09-18 기준)

> 목적: **다음 세션이 아무 항목이나 집어 들어 바로 착수**할 수 있게 만든 목록이다.
> 각 항목은 `무엇 / 왜 / 어디서 시작 / 선행조건 / 근거` 다섯 칸을 갖는다.
>
> 상위 문서 — 확정 계획·실측·결정 이력은 [PLAN.md](PLAN.md) 최상단(1~8차 확정),
> 인수인계는 [CONTEXT.md](CONTEXT.md) 최상단.
>
> 완료분: **G1·G2 훅/어퍼 · G3 돌진 · G5 잡기 · G6 인터럽트 창 · G7 Wells 폭탄 제거**
> (`b326e63d` 까지 `development` 반영). Play 검증은 팀장이 2026-09-18 에 수행했다.

---

## 🟢 바로 착수 가능 (선행조건 없음)

### B1. G4 — 점프 이륙 ⬅ **추천 1순위**

| | |
|---|---|
| **무엇** | 점프 공격에 **이륙 구간**을 넣는다. `Leap`(`Boss_23_jump`)을 3배속(1.90 → 0.633초)으로 재생하고, **모델 숨김·무적을 그 뒤로 미룬다.** |
| **왜** | 지금은 상승이 없다 — 모델을 즉시 숨기고 1.2초 뒤 순간이동한다. 팀장 지적: *"올라가는 애니가 있는데 그게 없이 순간이동으로 하늘로 올려버린다."* |
| **어디서 시작** | **[PLAN.md](PLAN.md) §3-A** — 실측값·구현 순서 7단계·함정 4개가 전부 적혀 있다. 재측정 불필요. |
| **선행조건** | 없음 |
| **크기** | 작음 (SO 필드 2개 + `BeginJump` 분해 + 단계 1개) |

🔴 §3-A 의 함정 중 셋이 특히 틀리기 쉽다 — 재생속도 상수 박기 금지(클립 길이에서 역산) ·
`_stateTimer` 예산에 이륙 몫 더하기 · 예고 `growTime` 을 `이륙 + 체공` 으로 늘리기.

### B2. 데미지 밸런스

| | |
|---|---|
| **무엇** | 공격별 데미지를 실제 값으로 채운다. |
| **왜** | **8개 공격 전부 `damage: 0`** 이다. 코드가 `e.damage > 0 ? e.damage : AttackDamage` 로 폴백하므로 훅·어퍼·대시·점프·잡기가 **전부 동일한 10 데미지**다. 의도된 임시 균등이고(팀장 확정) 밸런스는 미착수. |
| **어디서 시작** | `Assets/2.Prefabs/Monster/Data/No23.asset` 의 `attacks[].damage`. 잡기 전용은 `grabSlamDamage`/`grabTickDamage`/`grabThrowDamage`. |
| **선행조건** | 없음 (데이터만) |
| **크기** | 작음. 다만 **Play 튜닝 반복**이 본체다 |

### B3. 관찰된 불일치 2건 정리

| | |
|---|---|
| **무엇** | ① `jumpLandingDuration: 1` 인데 착지 클립 `Boss_23_landingattack` 은 **1.92초** — 코드가 절반에서 끊는다. ② `jumpSearchRadius` 가 이제 **점프·돌진 공용**인데 이름이 점프 전용처럼 읽힌다. |
| **왜** | ①은 착지 연출이 잘려 보일 수 있다(의도인지 확인 필요). ②는 다음 사람이 "돌진은 왜 점프 값을 보나"로 헤맨다. |
| **어디서 시작** | ① `No23.asset` + `TwentyThreeBoss.JumpLanding`. ② `BossDataSO.jumpSearchRadius` → `attackSearchRadius` 류로 개명(참조 2곳: `FindFarthestPlayer`). |
| **선행조건** | 없음 |
| **크기** | 아주 작음 |

---

## 🟡 남의 손이 필요 (선행조건 있음)

### B4. 🔴 몬스터 3종 오버레이 에셋 복구 — **민경 담당**

| | |
|---|---|
| **무엇** | 존재하지 않는 에셋 2개를 참조하는 프리팹 3종을 고친다 — `GauntletBot` · `SpinnerBot` · `WallBot`. |
| **왜** | 머티리얼 guid `98b1c99dee6c7b5488abe440aed99c45` 와 스크립트 guid `c7a41f60d2b84e6a9c15d380be720063`(`InterruptOverlay`)가 **디스크에 없고 git 이력에도 `.meta` 0건 — 한 번도 커밋된 적이 없다.** 프리팹이 `m_Materials.Array.size: 2` 로 슬롯을 늘리고 슬롯 1 을 비워 두므로 **서브메시가 렌더되지 않는다**(파편처럼 보인다). |
| **어디서 시작** | 유입 커밋 `5092995b`(인터럽트 섬광 배선 4종) · `cab1bf66`(RPC 통일). 23호는 모델 재스왑으로 이미 해소됐고 **나머지 3종이 남았다.** |
| **선행조건** | 🔴 **민경 님이 두 에셋을 올려야 풀린다.** 우리가 대체 에셋을 만들면 나중에 충돌한다. |
| **크기** | 우리 쪽은 0 (알리기만) |

⚠️ `development` 문제이므로 **받는 사람 전원이 같은 증상을 본다.** 우리 브랜치 문제로 오진되지 않게 따로 공유할 것.

### B5. 자폭 드론 공격 (Wells)

| | |
|---|---|
| **무엇** | 기획의 드론 공격 — 플레이어 1명에 표식 → 표식 종료 시 위치 고정 → 상공에서 돌진 → 범위 피해. 적중 수만큼 보스 고유 게이지 증감. |
| **왜** | G7 에서 폭탄 투척을 제거해 **Wells 가 지금 아무것도 안 한다**(장식). |
| **어디서 시작** | **자리는 이미 비워 뒀다** — `TwentyThreeBoss.OnWellsAttackCycle()`. 주기(`bombThrowInterval`)와 그로기·사망 억제가 그대로 살아 있다. 폭탄 시절의 착지점 추첨·탄도 역산이 필요하면 `7e9a242f` 직전 커밋에서 되살릴 수 있다. |
| **선행조건** | 🔴 **VFX 리소스 + "언제 쓰는지" 기획 확정**(팀장이 주기로 함). 게이지 증감은 B6 에 종속. |
| **크기** | 중간 |

### B6. 보스 고유 게이지 + 고출력 상태

| | |
|---|---|
| **무엇** | 게이지가 차면 짧은 고출력 상태 — **새 공격이 아니라 기존 공격을 쉬지 않고 연결**한다(공격 속도는 그대로, 패턴 간 정지시간만 제거). |
| **왜** | 기획 문서의 핵심 축인데 **코드에 한 줄도 없다**(전수 검색 0건). |
| **어디서 시작** | 선택기(`SelectAttackSlot`)의 쿨다운·연속감쇠와 `GlobalAttackInterval` 이 손댈 지점. 기획의 연결 예시 4종(돌진→훅 / 점프→어퍼 / 훅→반대편 어퍼 / 근접→돌진). |
| **선행조건** | 🔴 **상세 설계 없음**(팀장: "자세한 설계가 없어서 추후에 이야기"). 그리고 **B1~B3 이 먼저** — 연결할 패턴 자체가 아직 문서대로가 아니면 고출력의 "패턴 체인"이 의미가 없다. |
| **크기** | 큼 (보스 전투 루프 재설계) |

### B7. 과충전 3연속 돌진의 전기 지대

| | |
|---|---|
| **무엇** | 송전기 기믹 실패 시 3연속 돌진이 지나간 자리에 전기 지대를 남기고, 끝나면 동시 폭발. |
| **왜** | `rageDashCount: 3` 은 이미 있는데 **장판을 안 남긴다.** |
| **어디서 시작** | 인프라는 있다 — `AreaZone` + `AreaZoneType.Lightning` + `AreaZone.SpawnOrGrow`. 현재 호출부는 폭탄·차징 2곳뿐이고 `chargeZonePrefab` 은 비어 있다. `BeginRageDash`/`StopRageDash` 에 스폰을 붙이면 된다. |
| **선행조건** | 🔴 **설계가 덜 나왔고 이펙트도 없다**(팀장 확정 S8). |
| **크기** | 중간 |

---

## 🔵 기술 부채 (급하지 않지만 남겨 둔 근거가 있음)

### B8. 직렬화 예외 2670건 / Visual Scripting 정리

`Unable to find type: 'BossStateChanged'` 등이 **보스 씬 로드마다 2670건** 뜬다.
원인 확정: 우리 브랜치가 `f6175811` 에서 지운 **레거시 BT 타입**들이고, 이를 참조하는
`Assets/Unity.VisualScripting.Generated/.../UnitOptions.db` 는 **git 추적 대상도 아닌 생성 캐시**다.
컴파일·플레이를 막지 않는다. 🔴 **`development` 에 올라갔으니 팀원 전원이 이 로그를 본다.**
착수 시 첫 질문: **이 프로젝트가 Visual Scripting 을 실제로 쓰는가?** 안 쓰면 패키지 제거가 근본 해결이고,
그건 `Packages/manifest.json` 핀 변경이라 🔴 **Unity 를 닫고 해야 한다**(CLAUDE.md).

### B9. Codex 교차검증에서 짚혔으나 고치지 않은 4건

근거는 [PLAN.md](PLAN.md) 5차 확정 절 하단에 그대로 있다.

1. **`MonsterMeleeAttack._windowHits` 가 피해 성공 전에 기록된다** — 무적으로 막아도 그 공격의 1회를
   소비한다. 의미가 "성공 피해 1회"가 아니라 "접촉 시도 1회"다.
   🔴 **다른 몬스터도 쓰는 공용 컴포넌트**라 단독으로 뒤집지 않았다. **팀장 판단 대기.**
2. `EndDashMove` 가 `agent.enabled` 일 때만 복원하는데 저장값은 무조건 지운다 — 전진/돌진 중 사망하면
   가속도·autoBraking 원본을 잃는다. 기존 결함이고 보스는 곧 파괴되므로 무증상.
3. 늦게 접속한 클라가 예고·자세 홀드를 복구하지 못한다(RPC 는 재전달되지 않는다).
   `Docs/tech/networking.md` 가 **게임 시작 후 참가를 금지**하므로 현재 규약에서는 조건부.
4. 클라에서 직접 디스폰되면 로컬 자세 홀드가 남는다. 보스는 파괴되므로 지금은 무해 — **풀링을 도입하면 터진다.**

### B10. `PLAN.md` / `CONTEXT.md` 비대

머지 보존 절이 쌓여 각각 **4000줄에 가깝다.** 옛 절에 살아 있는 실측값이 섞여 있어
기계적으로 지울 수 없다 — 골라내는 작업이 필요하다. 대공사 때 같이 할 것.

---

## 착수 전 공통 확인 (매번)

- `CLAUDE.md` 세션 시작 규칙대로 `AGENTS.md` · `CONTEXT.md` · `PLAN.md` · `AIRULE.md` 를 먼저 읽는다.
- 🔴 **에셋을 에디터 밖에서 바꾸기 전에 물어라.** 디렉터리 이동·패키지 핀 변경·SVN 아트 교체·
  `.meta` guid 변경·머지는 **Unity 를 닫아야 한다**(CLAUDE.md §6).
- 🔴 **컴파일 검증은 `observedAssemblyCount` 를 확인하라.** 0 이면 그 사이클은 아무것도 검증하지 않았다
  (교훈 #106). 확실히 보려면 `unity_get_type_symbols` 로 컴파일된 어셈블리를 직접 조회한다.
- 🔴 **`.fbx.meta` 의 이벤트 `time` 은 정규화(0~1)이지 초가 아니다**(교훈 #105). 프레임레이트는
  FBX 의 `TimeMode`/`CustomFrameRate` 에서 뽑는다(23호 = 60fps).
- 예고와 판정은 **같은 SO 칸**에서 나와야 한다(`coneRadius`/`coneAngle`/`coneOffsetAngle`).
  한쪽만 고치면 "장판 밖인데 맞는" 버그가 된다.
